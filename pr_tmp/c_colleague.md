### 👥 Teammate — Notes

- 💡 The letter-by-letter name reveal is a nice touch — matches the Hero cascade language well.
- 💡 Could optionally show the splash only once per session (`sessionStorage`) so repeat visits skip it — non-blocking, current every-load behavior is fine for a portfolio.
- 💡 Nice call resolving the photo through `asset()` — easy to forget with the `/Portfolio/` base.
- 👍 GitMind is a strong addition; the open-source + tests angle reads well.

LGTM.
